import { NextPage } from 'next';

const PrivacyPolicy: NextPage = () => {
  return (
    <p>
      Privacy Policy: This is a demo app, so no data is collected.
    </p>
  );
};

export default PrivacyPolicy;